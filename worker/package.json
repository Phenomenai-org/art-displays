{
  "name": "phenomenai-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxy for Anthropic + ElevenLabs used by the Phenomenai installations.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
